# ⚠️ 本目录已废弃（未打包进卡）

这 27 个「段条目（段00-27）」文件是旧的「段」粒度事件库，日期为旧体系（段02=2110年4月/5月，比段01的7月早三个月）。

现状：
- 未进 `tavern-cards-state.json` 的 entryManifest，**不会打包进卡**。
- 已被「幕条目（`世界书/事件/幕条目/第1-133幕.md`，7月/9月体系）」取代。
- 写卡/打包一律以 **幕条目 + `世界书/时间线/剧情索引.yaml`** 为准。

注意：
- `patches/` 下的 `add-事件段00-03.json` ~ `add-事件段23-27.json` 是历史补丁，从零重建时会引用本目录；`remove-段条目.json` 已把段条目从 entryManifest 移除。
- 若要删除本目录，需先确认那些 add-事件段 补丁已废弃/不再用于重建，否则重建会报错。
